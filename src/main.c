/* STM32 CAN Gateway — main entry point
 * Board: STM32F4 Discovery
 * Build: CMake + arm-none-eabi-gcc
 */

int main(void)
{
    while (1)
    {
        /* Main loop — FreeRTOS scheduler will take over here */
    }
    return 0;
}